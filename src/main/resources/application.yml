soring:
  profiles:
    active: @spring.profiles.active@

spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/aws_names?useSSL=false&useTimezone=true&serverTimezone=UTC
    username: root
    password: 1234
